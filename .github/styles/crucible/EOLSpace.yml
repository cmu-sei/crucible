#
# Checks that there is no useless space at the end of lines.
#
# For a list of all options, see https://vale.sh/docs/topics/styles/
extends: existence
message: "Remove space characters from the end of the line."
vocab: false
level: error
scope: raw
raw:
  - ' +\n'
